# =============================================================================
# Rewine Backend - Environment Variables Example
# =============================================================================
# Copy this file to .env and customize for your environment.
# DO NOT commit .env files with actual secrets to version control.
#
# NOTE: Most configuration defaults are already set in the YAML files:
#   - application-local.yml (default profile for local dev)
#   - application-test.yml (for H2 testing)
#   - application-production.yml (for production)
#
# Environment variables here override YAML settings when set.
# =============================================================================

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=rewine
DB_USER=rewine
DB_PASSWORD=rewine_secret

# Server Configuration
SERVER_PORT=8080

# JWT Configuration
# IMPORTANT: Change this in production to a secure random string (min 256 bits)
JWT_SECRET=change-this-to-a-secure-secret-in-production-minimum-256-bits
JWT_ISSUER=rewine-backend
JWT_ACCESS_EXPIRATION=900000
JWT_REFRESH_EXPIRATION=604800000

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:4173,http://localhost:5173

# =============================================================================
# AI Configuration (OpenAI)
# =============================================================================
# AI settings can be configured in two ways:
#   1. Set environment variables here (recommended for secrets)
#   2. Edit application-local.yml directly (for non-secret settings)
#
# For local development, you can either:
#   - Set OPENAI_API_KEY here and load via .env
#   - Or set directly in application-local.yml (not recommended for secrets)
# =============================================================================

# Provider options: mock (default), openai
AI_PROVIDER=openai

# Set to false to disable AI features entirely
AI_ENABLED=true

# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# OpenAI model to use (default: gpt-4o-mini)
# Options: gpt-4o-mini, gpt-4o, gpt-4-turbo, gpt-3.5-turbo
OPENAI_MODEL=gpt-4o-mini

# Max tokens for AI responses (default: 2000)
OPENAI_MAX_TOKENS=2000

# Temperature for generation (0.0 - 2.0, default: 0.7)
# Lower = more deterministic, Higher = more creative
OPENAI_TEMPERATURE=0.7

# HTTP client settings for AI calls
AI_CONNECT_TIMEOUT=10
AI_READ_TIMEOUT=60
AI_MAX_RETRIES=2

# =============================================================================
# Logging Levels
LOG_LEVEL_APP=INFO
LOG_LEVEL_SQL=WARN

# Spring Profile
# Options: local (default), test, integration, production
SPRING_PROFILES_ACTIVE=local

