package com.rewine.backend.service;

/**
 * Interface for event service operations.
 */
public interface IEventService {

    // Placeholder - event service methods will be defined here
    // - findById(id)
    // - findAll(pageable, filters)
    // - findNearby(latitude, longitude, radius)
    // - create(eventData)
    // - update(id, eventData)
    // - delete(id)
    // - registerAttendee(eventId, userId)
    // - unregisterAttendee(eventId, userId)
}

