package com.rewine.backend.service;

/**
 * Interface for user service operations.
 */
public interface IUserService {

    // Placeholder - user service methods will be defined here
    // - findById(id)
    // - findByEmail(email)
    // - findAll(pageable, filters)
    // - update(id, userData)
    // - delete(id)
    // - updatePreferences(id, preferences)
    // - changePassword(id, oldPassword, newPassword)
    // - updateRole(id, role)
}

