package com.rewine.backend.service;

/**
 * Interface for wine command operations (write).
 */
public interface IWineCommandService {

    // Placeholder - wine command service methods will be defined here
    // - create(wineData)
    // - update(id, wineData)
    // - delete(id)
    // - addToComparison(wineIds)
    // - generateAiProfile(wineId)
}

