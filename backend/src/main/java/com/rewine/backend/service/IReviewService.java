package com.rewine.backend.service;

/**
 * Interface for review service operations.
 */
public interface IReviewService {

    // Placeholder - review service methods will be defined here
    // - findByWineId(wineId, pageable)
    // - findByUserId(userId, pageable)
    // - create(reviewData)
    // - update(id, reviewData)
    // - delete(id)
    // - calculateAverageRating(wineId)
}

