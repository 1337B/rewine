package com.rewine.backend.service;

/**
 * Interface for wine route service operations.
 */
public interface IWineRouteService {

    // Placeholder - wine route service methods will be defined here
    // - findById(id)
    // - findAll(pageable, filters)
    // - findByRegion(region)
    // - create(routeData)
    // - update(id, routeData)
    // - delete(id)
    // - addStop(routeId, stopData)
    // - removeStop(routeId, stopId)
    // - reorderStops(routeId, stopOrder)
}

