package com.rewine.backend.service;

/**
 * Interface for wine query operations (read-only).
 */
public interface IWineQueryService {

    // Placeholder - wine query service methods will be defined here
    // - findById(id)
    // - findAll(pageable, filters)
    // - search(query, pageable)
    // - findByWinery(wineryId)
    // - findByRegion(region)
}

