package com.rewine.backend.controller;

/**
 * Interface for event endpoints.
 */
public interface IEventController {

    // Placeholder - event endpoints will be defined here
    // - GET /events (list with pagination and filters)
    // - GET /events/{id} (details)
    // - POST /events (create)
    // - PUT /events/{id} (update)
    // - DELETE /events/{id} (delete)
    // - POST /events/{id}/register (register for event)
    // - GET /events/{id}/attendees (list attendees)
}

