package com.rewine.backend.controller;

/**
 * Interface for wine route endpoints.
 */
public interface IWineRouteController {

    // Placeholder - wine route endpoints will be defined here
    // - GET /wine-routes (list with pagination and filters)
    // - GET /wine-routes/{id} (details)
    // - POST /wine-routes (create)
    // - PUT /wine-routes/{id} (update)
    // - DELETE /wine-routes/{id} (delete)
    // - POST /wine-routes/{id}/stops (add stop)
    // - DELETE /wine-routes/{id}/stops/{stopId} (remove stop)
}

