package com.rewine.backend.controller;

/**
 * Interface for wine endpoints.
 */
public interface IWineController {

    // Placeholder - wine endpoints will be defined here
    // - GET /wines (list with pagination and filters)
    // - GET /wines/{id} (details)
    // - POST /wines (create)
    // - PUT /wines/{id} (update)
    // - DELETE /wines/{id} (delete)
    // - POST /wines/compare (AI comparison)
    // - POST /wines/{id}/ai-profile (AI sommelier profile)
    // - POST /wines/scan (wine label scanning)
}

